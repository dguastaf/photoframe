PHOTO_SOURCE=photoprism # There may be other sources in the future.

# Reachable from the app container (use host LAN IP if Photoprism runs on the Docker host).
PHOTOPRISM_BASE_URL=http://photoprism.local:2342
PHOTOPRISM_TOKEN=replace-me

# CORS_ORIGINS: optional; when unset, defaults to the client dev origin in config/ports.json.
# Not needed for docker compose — browser uses http://<host>:6389 and nginx proxies API routes.
