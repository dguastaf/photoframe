/** Default slideshow display duration (PRD: 1 minute). */
export const DEFAULT_DISPLAY_MS = 60_000

/** Refetch the photo catalog and restart playback after this interval. */
export const LIBRARY_REFRESH_MS = 24 * 60 * 60 * 1000
