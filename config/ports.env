# Canonical port defaults for Photoframe (server, Vite dev UI, Docker).
# Override in .env or the shell; compose loads this via env_file.
PHOTOFRAME_SERVER_PORT=52525
PHOTOFRAME_CLIENT_PORT=6389
PHOTOFRAME_CLIENT_HOST=localhost
