---
description: Require screenshots or videos on PRs when UI files change
globs: client/**/*
alwaysApply: false
---

# UI preview assets for pull requests

When you change UI files (`client/src/**`, `client/index.html`, `client/public/**`), the pull request must include refreshed assets under `.github/ui-preview/`. CI blocks PRs that change UI without an updated screenshot or video; individual commits do not need to include assets.

## Screenshot vs video

| Change type | Command | Output |
| --- | --- | --- |
| Any UI change (default) | `cd client && npm run ui:preview` | both files + validation |
| Layout / copy only | `cd client && npm run ui:screenshot` | `app-shell.png` (also refresh video if behavior changed) |
| Slideshow / navigation / timers | `cd client && npm run ui:preview` | must show slide change in `app-flow.webm` |

**Do not** commit `ui:video` alone after slideshow/timer changes — it produces a ~15KB useless clip. `ui:preview` runs capture then `validate.mjs` (min size/duration).

Run capture **before opening the PR** (any commit on the branch may include the assets).

## Opening / updating a PR

1. Ensure the branch (vs base) includes updated preview assets whenever UI source changed.
2. **Never** paste `![...](.github/ui-preview/app-shell.png)` — GitHub shows a broken image. After capture, run:

```bash
cd client && npm run ui:embed
```

Copy the printed block into the PR description (screenshot uses `raw.githubusercontent.com`). `ui:preview` prints the same block when capture finishes.

3. Do not open a PR with UI-only changes and no updated preview files — CI will fail. CI also fails if docs reintroduce relative `.github/ui-preview/` image links (`lint-docs-embed.sh`).

First-time setup on a machine: `npx playwright install chromium` from `client/`.
